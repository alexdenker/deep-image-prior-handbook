channels: [128, 128, 128, 128, 128, 128]
scales: 6
skip: [0, 0, 0, 0, 0, 0]
activation: 'leaky_relu' # relu silu leakyrelu
padding_mode: 'zeros' # circular zeros
upsample_mode: 'nearest' # nearest bilinear (note that bilinear is not reproducible on GPU)
use_norm: True
use_sigmoid: True 